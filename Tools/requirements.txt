# The reader. Chatterbox Multilingual is MIT for both the code and the weights,
# which is the reason it was chosen: the audio it produces can be given away
# with the addon. See README.md for the ones that cannot be.
chatterbox-tts>=0.1.4
torch>=2.4
torchaudio>=2.4
# ffmpeg is a separate install and must be on PATH; it encodes the Ogg Vorbis
# the WoW client plays.

# Chatterbox watermarks its output through resemble-perth, which still imports
# pkg_resources. Setuptools 81 dropped that, so the watermarker silently becomes
# None and the model raises on construction with a bare "NoneType is not
# callable". Pin below 81 until perth stops using it.
setuptools<81
